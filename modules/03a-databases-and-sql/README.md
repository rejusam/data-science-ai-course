# Databases and SQL

Plan label: `Module 3 Pt 1`. Week 6.

## Sessions

| Date | Focus | Lab / activity |
|---|---|---|
| Wed, 9 Sep 2026 | Databases and relational modelling; SQL SELECT, JOIN, GROUP BY, subqueries | Lab SQL |
| Sat, 12 Sep 2026 | SQL in Python (SQLAlchemy); APIs: HTTP, REST, JSON, authentication, rate limits | Labs SQL-in-Python and first API call; MP1 checkpoint 1 (data sourced) |

## What this module covers

- Databases and relational modelling
- SQL SELECT, JOIN, GROUP BY, subqueries
- SQL in Python (SQLAlchemy)
- APIs: HTTP, REST, JSON, authentication, rate limits

## Labs and activities

- Lab SQL
- Labs SQL-in-Python and first API call; MP1 checkpoint 1 (data sourced)

## How to get the most out of these sessions

- Write the query before running it; predict the row count
- Show your messy work-in-progress at the checkpoint - early feedback saves rework

## Folders

- `notebooks/` — worked examples and lab notebooks
- `data/` — datasets used by this module

## Before you start

Activate the course environment:

```
conda activate dsai
```

Then start Jupyter from the repository root:

```
jupyter lab
```
